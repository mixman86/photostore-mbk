import ToolbarHeader from './ToolbarHeader'

const BaseHeader = () => {
  return (
    <ToolbarHeader title="Matthew Bowker Photography" menuOptions={['Home', 'About', 'Contact']} />
  )
}

export default BaseHeader